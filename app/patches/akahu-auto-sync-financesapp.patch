diff --git a/packages/desktop-client/src/components/FinancesApp.tsx b/packages/desktop-client/src/components/FinancesApp.tsx
index 19e0367..5226f51 100644
--- a/packages/desktop-client/src/components/FinancesApp.tsx
+++ b/packages/desktop-client/src/components/FinancesApp.tsx
@@ -13,6 +13,7 @@ import { getLatestAppVersion, sync } from '#app/appSlice';
 import { ProtectedRoute } from '#auth/ProtectedRoute';
 import { Permissions } from '#auth/types';
 import { useAccounts } from '#hooks/useAccounts';
+import { useAkahuAutoSync } from '#hooks/useAkahuAutoSync';
 import { useGlobalPref } from '#hooks/useGlobalPref';
 import { useLocalPref } from '#hooks/useLocalPref';
 import { useMetaThemeColor } from '#hooks/useMetaThemeColor';
@@ -96,6 +97,9 @@ export function FinancesApp() {
 
   const { data: accounts, isFetching: isAccountsFetching } = useAccounts();
 
+  // TRACKIE: auto-sync linked bank accounts on budget open (20h per-user gate).
+  useAkahuAutoSync();
+
   const versionInfo = useSelector(state => state.app.versionInfo);
   const [notifyWhenUpdateIsAvailable] = useGlobalPref(
     'notifyWhenUpdateIsAvailable',
@@ -119,7 +123,7 @@ export function FinancesApp() {
         addNotification({
           notification: {
             type: 'message',
-            title: t('A new version of Actual is available!'),
+            title: t('A new version of Trackie is available!'),
             message: t(
               'Click the button below to reload and apply the update.',
             ),
@@ -155,7 +159,7 @@ export function FinancesApp() {
           addNotification({
             notification: {
               type: 'message',
-              title: t('A new version of Actual is available!'),
+              title: t('A new version of Trackie is available!'),
               message:
                 (import.meta.env.REACT_APP_IS_PIKAPODS ?? '').toLowerCase() ===
                 'true'
@@ -163,7 +167,7 @@ export function FinancesApp() {
                       'A new version of Actual is available! Your Pikapods instance will be automatically updated in the next few days - no action needed.',
                     )
                   : t(
-                      'Version {{latestVersion}} of Actual was recently released.',
+                      'Version {{latestVersion}} of Trackie was recently released.',
                       { latestVersion: versionInfo.latestVersion },
                     ),
               sticky: true,
