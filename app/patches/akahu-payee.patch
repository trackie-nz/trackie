diff --git a/packages/sync-server/src/app-akahu/app-akahu.ts b/packages/sync-server/src/app-akahu/app-akahu.ts
index 86a4f3e..e4e55f3 100644
--- a/packages/sync-server/src/app-akahu/app-akahu.ts
+++ b/packages/sync-server/src/app-akahu/app-akahu.ts
@@ -310,7 +310,12 @@ function getPayeeName(
     }
   }
 
-  return '';
+  /* Trackie: fall back to the raw bank description (Akahu's narrative, straight
+     from the bank) when there is no enriched merchant. Without this, every
+     non-enriched transaction - direct debits, automatic payments, EFTPOS Akahu
+     did not merchant-match - imports with a blank payee. Mirrors the de-facto
+     Akahu->Actual community mapping (merchant.name ?? description). */
+  return trans.description ?? '';
 }
 
 function processPendingTransaction(
