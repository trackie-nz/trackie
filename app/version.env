# Single source of truth for the upstream Actual release the overlay targets.
# Read by ci.yml, release.yml, tag.sh and assert-overlay.sh. Bump this (and
# regenerate the patches) to track a new upstream version.
ACTUAL_VERSION=26.7.0
